---
description: Githubへ変更をプッシュする (add -> commit -> push)
---

1. `git status` を実行して現在の変更状況を確認します。
2. `git add .` を実行して全ての変更をステージングエリアに追加します。
3. コミットメッセージをユーザーに確認するか、汎用的なメッセージ（例: "update"）を使用して `git commit -m "メッセージ"` を実行します。
4. `git push` を実行してリモートリポジトリに変更を送信します。
